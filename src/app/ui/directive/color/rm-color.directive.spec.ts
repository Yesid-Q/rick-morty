import { RmColorDirective } from './rm-color.directive';

describe('RmColorDirective', () => {
  it('should create an instance', () => {
    const directive = new RmColorDirective();
    expect(directive).toBeTruthy();
  });
});
